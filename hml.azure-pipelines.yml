# Variables: ["shared-var-group", "hml-var-group"]

trigger:
  - homolog

resources:
  repositories:
  - repository: templates
    type: git
    name: templates

jobs:
- template: pipelines/func-app.node.azure-pipelines.yml@templates
